# Binaries
*.exe

# Runtime logs
avatar.log

# Config with secrets (keep cfg.yml.template as template)
cfg.yml

# Build artifacts
dist/

# Offline model files (large binaries, distributed separately via zip).
# Everything under models/ is ignored except the README.
models/*
!models/README.md

# JS obfuscation backup (temporary, created/restored by build.ps1)
web/index.html.orig

# go-winres intermediate object (rsrc_windows_amd64.syso is the committed copy)
rsrc

# Self-signed certificate (generated by build.ps1)
cert/

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/